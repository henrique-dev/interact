import { Layout } from '@/components/layout';
import { HomePage } from '@/components/pages/home';

const Page = () => {
  return (
    <Layout>
      <HomePage />
    </Layout>
  );
};

export default Page;
